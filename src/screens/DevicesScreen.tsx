import {Text} from 'react-native';
import React from 'react';
import AppContainer from '../components/AppContainer';

const DevicesScreen = () => {
  return (
    <AppContainer>
      <Text style={{color: 'white'}}>DevicesScreen</Text>
    </AppContainer>
  );
};

export default DevicesScreen;
